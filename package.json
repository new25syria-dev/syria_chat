{
  "name": "fadfad-socket-server",
  "version": "1.0.0",
  "description": "Socket.io server for FadFad app with PostgreSQL persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "socket.io": "^4.8.1"
  }
}
